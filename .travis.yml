language: python
python:
  - "3.7-dev"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - keeper
  - logger
  - replicator
  - sender
  - executer
